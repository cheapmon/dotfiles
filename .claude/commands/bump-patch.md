# Bump Patch Version

Fetch the latest semantic version tag, increment the patch version, create the new tag, and push it to remote.

Steps:
1. Fetch all tags from remote
2. Find the latest semantic version tag (format: vX.Y.Z)
3. Increment the patch version
4. Create an annotated tag with the new version
5. Push the tag to origin
