[module/keyboard]
type = custom/script
exec = KEEB=$(upower --dump | rg keyboard -A 7 | rg percentage | cut -d: -f2 | xargs); echo "${KEEB:-?}"
interval = 1

format =   <label>
format-background = ${colors.bg}
